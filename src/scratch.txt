
class MyEntry(BoxLayout):
    orientation = 'horizontal'
    # padding = 20
    spacing = 10

    def build(self):
        print('Test')
        lbl = Label()
        lbl.id = 'lbl'
        lbl.text = datetime.now()
        self.add_widget(lbl)
        img = Image()
        img.id = 'img'
        img.source = 'sbahn_Transparent.png'
        img.size = img.texture_size
        self.add_widget(img)
        return self


<MyEntry@BoxLayout>:
    orientation:'horizontal'
    padding:20
    spacing:10

    Image:
        size: self.texture_size
        id: 'img'
        source: 'sbahn_Transparent.png'
    Label:
        id: 'lbl'
        # text:"[size=60][color=55bb33]SBahn[/color][/size]"
        markup: True